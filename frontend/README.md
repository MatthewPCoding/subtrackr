# Subtrackr — Frontend

React Native (Expo) mobile app. See the [root README](../README.md) for full setup instructions and project overview.

## Quick start

```bash
npm install
npx expo start
```

For OAuth flows (email connect, sign in with Google), a development build is required:

```bash
npx expo run:ios
```
